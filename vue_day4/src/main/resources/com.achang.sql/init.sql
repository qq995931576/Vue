CREATE TABLE t_user(
                       id VARCHAR(40) PRIMARY KEY,
                       NAME VARCHAR(40),
                       age INT(3),
                       salary DOUBLE(7,2),
                       phoneNumber VARCHAR(11)
);